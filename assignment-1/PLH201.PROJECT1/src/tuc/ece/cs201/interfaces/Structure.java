package tuc.ece.cs201.interfaces;

public interface Structure {  //An interface for inserting and searching points (x, y) in data structures and file structures
	public void insert(int x, int y);
	public boolean search(int x, int y);
}
